# pnpm 11 settings — must live here, not in package.json.
# We're in an isolated docker stage building a vetted lockfile from a public
# repo; loosen pnpm's defensive defaults so deps install cleanly.
minimumReleaseAge: 0
onlyBuiltDependencies:
  - esbuild
dangerouslyAllowAllBuilds: true
