# pnpm 11+ rejects packages published within the last 24h by default
# (ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION). Opt out — we pin via lockfile,
# which is the supply-chain anchor that matters for reproducible builds.
minimum-release-age=0
